type Category {
  service: Service!
  title: String!
}

extend type Query {
  categories(service: Service!): [Category!]
}

query fetchCategories($service: Service!) {
  categories(service: $service) {
    service
    title
  }
}
